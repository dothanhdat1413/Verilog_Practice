#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

uint8_t z[64];
uint8_t d[32];
uint8_t q[32];
uint8_t r[32];

void sub(uint8_t a[32], uint8_t b[32]){
    
}

int main() {
    for(int i = 0; i < 34; i++) {
        
    }
}